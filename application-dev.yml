spring:
  config:
    activate:
      on-profile: dev
  application:
    name: care-external
  redis:
    host: localhost
    port: 6379
    expire: 3000
  api:
    auth:

queryAPIURI: queryAPI
testAPIURI:

server:
  port: 8080

management:
  security:
    enabled: false
  endpoints:
    web:
      exposure:
        include: "*"
